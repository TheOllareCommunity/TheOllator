# TheOllator
OllareTheGang

**git pull origin master** to update server

## Instructions
### Frontend
Run ```pip install simpleaudio``` from terminal if not playing

### Backend
Run first ```SET FLASK_APP=server.py``` (**win**) or ```export FLASK_APP=server.py``` (**mac**) and then ```flask run --port=8080``` from terminal to boot server. Alternatively ```python server.py``` simply also works and the script automatically sets the environment variables. 
